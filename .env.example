# Optional runtime configuration for macchanger_pro.
# Directory where original interface MAC addresses are backed up.
MACCHANGER_BACKUP_DIR=/var/lib/macchanger

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
MACCHANGER_LOG_LEVEL=INFO

